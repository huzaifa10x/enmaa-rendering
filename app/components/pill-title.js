import React from 'react'

export default function PillTitle({ title }) {
    return (
        <div className="border rounded-full bg-primary font-bold text-white px-4 py-1 uppercase tracking-widest text-sm inline-block mb-3">{title}</div>
    )
}
